# Pinned to the versions the benchmark was developed and validated against.
# See ENVIRONMENT.md (written at runtime) for the exact installed versions of a given run.
#
# torch is pinned to 2.3.0; the benchmark runs on CPU by default (a CPU-only wheel is fine).
# Dependabot flags some torch advisories, but none are reachable here (no torch.load / RNN / jit /
# quantized code paths are used) — see SECURITY.md. Pinned for bit-for-bit reproducibility.
# toorpia is OPTIONAL: it is only needed to (re)generate toorPIA embeddings via the remote API.
# Public users reproduce all figures/metrics from the committed external_embeddings/ caches WITHOUT
# a toorPIA key, so toorpia does not need to be installed to reproduce the published results.

numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.2
pandas==2.2.2
matplotlib==3.10.8
torch==2.3.0

# DR methods
umap-learn==0.5.12
pymde==0.3.0
pccdr==1.0.3
# DREAMS: the authors' openTSNE fork (installs AS openTSNE, version 1.0.2 + regularization;
# nothing else in this repo imports openTSNE, so the swap affects only methods/dreams_method.py).
# The tp head (eeea6a6) has a fit()-crashing bug patched at import time in the wrapper — see
# methods/dreams_method.py. Without this install, DREAMS is skipped gracefully (SkipMethod).
# pip install "git+https://github.com/berenslab/DREAMS.git@tp"

# tests
pytest>=7.0

# toorPIA API client (optional; install from git). Requires TOORPIA_API_KEY to call the API.
# pip install git+https://github.com/toorpia/toorpia.git   # ==1.2.0 (basemap_embedding required)
